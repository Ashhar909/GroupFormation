const initState = {
    error: null,
    group:null
}

const groupReducer = (state = initState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default groupReducer